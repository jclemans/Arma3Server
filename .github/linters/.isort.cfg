[settings]
profile = black
# isort classifies an import by whether it can resolve it from its working
# directory. Linters run from outside the repo root, which would otherwise
# split these between sections depending on where isort was invoked.
known_first_party = context,entrypoint,keys,launch,local,preflight,steam_auth,steamcmd,workshop
